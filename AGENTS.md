# Want Product Principles

1. ユーザーに考えさせすぎない
2. 起動後すぐ価値が出る
3. 入力を求めるより、ユーザーの行動そのものを記録する
4. 同じ候補を何度も見せない
5. 好みに最適化しすぎず、新しい候補も混ぜる
6. ストリーク、過剰通知、依存を促す仕組みは原則入れない
7. アプリ内滞在時間を伸ばすことを目的にしない
8. ユーザーを現実の行動へ送り出す
9. Home = 自分のWant
10. Discover = 新しいWantとの出会い
11. Learn = 自己理解
12. Active Wantは常に1つだけ

# UX rules

- iPhoneでの操作を最優先する
- 標準的で直感的なUIを優先する
- 不要なタップを増やさない
- 重要な操作は1〜2タップ以内を目安にする
- 横スクロールで選択肢が見切れるUIは避ける
- 「あとで」は重要なユーザーデータとして扱う
- 保存したWantへ簡単にアクセスできるようにする
- 既存のlocalStorageデータを壊さない

# Development rules

- GitHub Pagesで公開するWebアプリ
- index.htmlが現在の本番ファイル
- 大規模リファクタリングは必要になるまで行わない
- 新バージョンでは画面上に目立たない形でバージョン番号を表示する
- localStorageの構造を変更する場合は旧バージョンから移行処理を入れる
- JavaScript変更後は構文チェックを行う
- 実装後は変更点とテスト結果を簡潔に報告する
- 要求されていない機能を勝手に追加しない
